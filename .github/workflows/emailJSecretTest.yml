name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Get the key
        id: key
        run:
          NOW=${{ secrets.MY_PUBLIC_KEY }}
          echo "now${NOW}" >> $GITHUB_OUTPUT
